import styled from 'styled-components';

const Styling = styled.div`
    &.incorrect_answer{
        color:red;
    }
    &.correct_answer{
        color: green;
    }
`;

export default Styling;
